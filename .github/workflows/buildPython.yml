name: Build python files
on: push
jobs:
  Python_Build:
    runs-on: windows-latest
    steps:
      - name : Checking python files
        uses: actions/checkout@v4.2.2
      - name : run python
      - run : |
              ls
              cd Python
              ls
              python first.py
      
      
      
  
